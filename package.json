{
  "name": "media-extractor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A personal Chrome and Edge extension to find and download website media.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:browser": "node scripts/browser-test.mjs",
    "check": "node --check extension/background.js && node --check extension/scanner.js && node --check extension/app.js"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
