{
  "name": "media-extractor",
  "version": "8.0.0",
  "private": true,
  "type": "module",
  "description": "Download videos and photo posts from YouTube, Instagram, X, Reddit, TikTok and Facebook.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:browser": "node scripts/browser-test.mjs",
    "helper": "node helper/server.mjs",
    "check": "node --check extension/background.js && node --check extension/youtube.js && node --check extension/youtube-url.js && node --check helper/server.mjs && node --check helper/native.mjs && node --check helper/native-protocol.mjs && node --check extension/collect-post.js && node --check helper/scan-manager.mjs && node --check helper/scan-parser.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
