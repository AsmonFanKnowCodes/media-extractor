{
  "name": "media-extractor",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "A personal Chrome and Edge extension to find and download website media.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:browser": "node scripts/browser-test.mjs",
    "helper": "node helper/server.mjs",
    "check": "node --check extension/background.js && node --check extension/scanner.js && node --check extension/app.js && node --check extension/youtube.js && node --check helper/server.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}
